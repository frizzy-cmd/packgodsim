# packgodsim
**BOII SHUT YO GOOGLE CHROME CHICKEN BONE**

**May contain explicit content. (assets/ttc.gif), but im sure most insensitive people dont mind**

# You will need to ``pip install`` these to make the script work!
```
pip install pygame pillow
```
or alternatively, use
```
pip install pygame-ce pillow
```

# How to install?
**Clone this repository then open your terminal in the project directory and run the command to get all required libraries, after that, you can run python pack.py in the project directory.**
**..or, you could use .exe in Releases, But that's not fun now, is it?**
